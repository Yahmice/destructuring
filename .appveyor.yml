environment:
  nodejs_version: "18"

branches:
  only:
    - origin 
    - main

cache: node_modules  

install:
  - npm install  

build: off  

build_script:
  - npm run build  

test_script:
  - npm run test  